[package]
name = "r3fit"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9.1"
thiserror = "2.0.12"

[dev-dependencies]
criterion = { version = "0.5.1", features = ["html_reports"] }
proptest = "1.6.0"

[[bench]]
name = "fit_bench"
harness = false